{
  "name": "test-monorepo-sara-project",
  "version": "1.0.0",
  "description": "A repository to test the create-sara-project framework in a monorepo environment",
  "main": "index.js",
  "scripts": {
    "pre-commit": "npm run --prefix ./client/ pre-commit",
    "prepare": "husky install",
    "test": "npm run --prefix ./client/ test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "husky": "^8.0.3"
  }
}
